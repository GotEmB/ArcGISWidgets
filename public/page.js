// Generated by CoffeeScript 1.4.0

require(["dijit/registry", "dojo/ready", "dojo/dom", "dojox/layout/FloatingPane", "gotemb/ClassifyWidget", "dojo/parser", "dijit/layout/BorderContainer", "dijit/layout/ContentPane", "dijit/TitlePane", "esri/map", "esri/geometry", "esri/dijit/Attribution"], function(registry, ready, dom, FloatingPane, ClassifyWidget) {
  return ready(function() {
    var classifyWidget, classifyWidgetContainer, map, _ref;
    map = new esri.Map("map", {
      center: [-56.049, 38.485],
      zoom: 3,
      basemap: "streets"
    });
    if ((_ref = navigator.geolocation) != null) {
      _ref.getCurrentPosition(function(_arg) {
        var coords;
        coords = _arg.coords;
        return map.centerAndZoom(new esri.geometry.Point(coords.longitude, coords.latitude), 8);
      });
    }
    classifyWidgetContainer = new FloatingPane({
      title: "Classify Widget",
      resizable: false,
      dockable: false,
      closable: false,
      style: "height: 160px; width: 390px",
      content: (classifyWidget = new ClassifyWidget).domNode
    }, dom.byId("classifyWidgetContainer"));
    classifyWidgetContainer.startup();
    return classifyWidget.set("map", map);
  });
});
